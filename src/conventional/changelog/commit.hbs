*{{#if scope}} **{{scope}}:**
{{~/if}} {{subject}}
{{~#if hash}} {{#if @root.linkReferences~}}
  ([{{shortHash}}]({{@root.commitUrlFormat}}))
{{~else}}
  {{~shortHash}}
{{~/if}}{{~/if}}
{{~#if references~}}
  , closes
  {{~#each references}} {{#if @root.linkReferences~}}
    [
    {{~#if this.owner}}
      {{~this.owner}}/
    {{~/if}}
    {{~this.repository}}{{this.prefix}}{{this.issue}}]({{@root.issueUrlFormat}})
  {{~else}}{{this.prefix}}{{this.issue}}
  {{~/if}}{{/each}}
{{~/if}}

